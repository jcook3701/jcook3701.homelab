# PXE-Boot

## Installed Packages

* DNS (bind9)
* LDAP (openldap)
* HTTP (nginx) or FTP (vsftpd)
* SMB (samaba)

## Install Command

``` shell
$ ansible-playbook jcook3701.homelab.pxeboot.yml -K
```
