machine api.github.com login {{ github.username }}^grip password {{ vault.github_pat_token }}
machine api.github.com login {{ github.username }}^forge password {{ vault.github_pat_token }}
machine openai.com password {{ vault.openai_token }}

{# TODO: This should loop through email accounts and add all of them #}
machine imap.gmail.com login {{ }}  password {{ vault.google_token }}
machine smtp.gmail.com login {{ }} password {{ }}

{# TODO: This should loop through calendars and add all of them #}
machine apidata.googleusercontent.com login {{ }} password {{ vault.google_token }}
machine apidata.googleusercontent.com login {{ }} password {{ vault.google_token }}
