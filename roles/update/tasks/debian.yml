# SPDX-FileCopyrightText: Copyright © 2026, Jared Cook
# SPDX-License-Identifier: AGPL-3.0-or-later
---
- name: Debian - Update all packages to their latest version
  ansible.builtin.apt:
    name: "*"
    state: latest
#  register: apt_result

- name: Debian - Remove dependencies that are no longer required
  ansible.builtin.apt:
    autoremove: yes
# - name: Print return information from the previous installation task
#   ansible.builtin.debug:
#    var: apt_result

# - name: Print the env information
